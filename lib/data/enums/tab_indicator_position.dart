/// Describes where the tab indicator should be drawn
/// relative to the tab widget.
enum TabIndicatorPosition {
  top, // Indicator appears above the tab label/icon
  bottom, // Indicator appears below the tab label/icon
  overlay, // Indicator overlaps the entire tab area
}
